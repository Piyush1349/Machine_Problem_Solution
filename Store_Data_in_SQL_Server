Make the Information base and Table
Execute the accompanying SQL content to make the information base and table.

Make Information base SystemMonitoringDB;

USE SystemMonitoringDB;

Make TABLE MonitoringData (
    Id INT Essential KEY IDENTITY(1,1),
    Timestamp DATETIME DEFAULT GETDATE(),
    CpuUsage FLOAT,
    IpDetails NVARCHAR(255)
);
